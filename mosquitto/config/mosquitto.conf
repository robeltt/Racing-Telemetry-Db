# Racing Mosquitto config
# Lets your Python scripts connect without a username/password (fine for a local
# class project). For production you'd add authentication here.

listener 1883
allow_anonymous true

# Optional websockets listener (handy if you later add a browser dashboard)
listener 9001
protocol websockets
